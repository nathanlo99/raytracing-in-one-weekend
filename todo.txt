x cheap instance transformations
x transition to using matrices instead of ad hoc math
x improve BVHs
- add imgui
  x transfer code from dev/render_test
  - decouple the width and height from the rendering code
  - write a more reasonable main loop
- importance sampling
- triangle mesh import
  x write a triangle hittable class
  x import the vertices and faces of an obj model
  x import the uv coordinates of the obj model
  x import .mtl files
  - integrate normal / bump textures
- add scene lua support (can get inspiration from lux)
  - consider gltf
- spectral rendering